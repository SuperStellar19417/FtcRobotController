package org.firstinspires.ftc.teamcode.OpModes.testOpmodes;


public class temp {
    /*
    drive.trajectorySequenceBuilder(new Pose2d(60, -36, Math.toRadians(90)))
            .splineTo(new Vector2d(60, -60), Math.toRadians(-45))
            // lift up arm to low bucket level
            // drop sample into bucket
            .splineTo(new Vector2d(0, -60), Math.toRadians(90))
            .splineTo(new Vector2d(-0, -24), Math.toRadians(90))
            // touch/hang on the ascent bar
            .build();
     */
}
