package org.firstinspires.ftc.teamcode.OpModes.Action;

public class TemporaryRedRight {
  /* drive.trajectorySequenceBuilder(new Pose2d(60, 12, Math.toRadians(90)))
            .splineTo(new Vector2d(66, 54), Math.toRadians(90))
            move to the observation zone and pick up the speciman
            .splineTo(new Vector2d(30, 0), Math.toRadians(90))
            move to the chamebrs and use the arms & slides to hang the speciman
            .splineTo(new Vector2d(24, 48), Math.toRadians(90))
            pick up sample and give to the huan player
            .splineTo(new Vector2d(66, 60), Math.toRadians(90))
            use the arm to pick up the speciman
            .splineTo(new Vector2d(36, -0), Math.toRadians(90))
            move to the chamber and hang the speciman using the arm and slides
            .splineTo(new Vector2d(66, 48), Math.toRadians(90))
            move back to the observations zone and park
            .build()

   */
}
